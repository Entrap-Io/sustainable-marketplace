<%- include('../partials/header', { title: product.id ? 'Edit Product' : 'Add Product' }) %>

<div class="container mt-5" style="max-width: 600px;">
  <h2 class="mb-4"><%= product.id ? 'Edit Product' : 'Add New Product' %></h2>
  <form action="<%= action %>" method="POST" enctype="multipart/form-data">
    <div class="mb-3">
      <label>Title</label>
      <input type="text" name="title" class="form-control <%= errors.title ? 'is-invalid' : '' %>" value="<%= product.title || '' %>" required>
      <% if (errors.title) { %><div class="invalid-feedback"><%= errors.title.msg %></div><% } %>
    </div>
    <div class="mb-3">
      <label>Stock Quantity</label>
      <input type="number" name="stock" class="form-control <%= errors.stock ? 'is-invalid' : '' %>" value="<%= product.stock || '' %>" min="0" required>
      <% if (errors.stock) { %><div class="invalid-feedback"><%= errors.stock.msg %></div><% } %>
    </div>
    <div class="row mb-3">
      <div class="col">
        <label>Normal Price (TL)</label>
        <input type="number" step="0.01" name="normal_price" class="form-control <%= errors.normal_price ? 'is-invalid' : '' %>" value="<%= product.normal_price || '' %>" min="0" required>
        <% if (errors.normal_price) { %><div class="invalid-feedback"><%= errors.normal_price.msg %></div><% } %>
      </div>
      <div class="col">
        <label>Discounted Price (TL)</label>
        <input type="number" step="0.01" name="discounted_price" class="form-control <%= errors.discounted_price ? 'is-invalid' : '' %>" value="<%= product.discounted_price || '' %>" min="0" required>
        <% if (errors.discounted_price) { %><div class="invalid-feedback"><%= errors.discounted_price.msg %></div><% } %>
      </div>
    </div>
    <div class="mb-3">
      <label>Expiration Date</label>
      <input type="date" name="expiration_date" class="form-control <%= errors.expiration_date ? 'is-invalid' : '' %>" value="<%= product.expiration_date || '' %>" required>
      <% if (errors.expiration_date) { %><div class="invalid-feedback"><%= errors.expiration_date.msg %></div><% } %>
    </div>
    <div class="mb-3">
      <label>Product Image</label>
      <input type="file" name="image" class="form-control" accept="image/*" <%= product.id ? '' : 'required' %>>
      
      <% if (product.image_filename) { %>
        <small class="text-muted d-block mt-1">Current image: <%= product.image_filename %></small>
      <% } %>
    </div>
    <button type="submit" class="btn btn-primary w-100"><%= product.id ? 'Update Product' : 'Save Product' %></button>
    <a href="/market/dashboard" class="btn btn-secondary w-100 mt-2">Cancel</a>
  </form>
</div>
<%- include('../partials/footer') %>
